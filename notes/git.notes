# Create a bare repository
$ cd /opt/git
$ mkdir project.git
$ cd project.git
$ git --bare init


# upstream the project
$ cd myproject
$ git init
$ git add .
$ git commit -m 'initial commit'
$ git remote add origin git@gitserver:/opt/git/project.git
$ git push origin master
